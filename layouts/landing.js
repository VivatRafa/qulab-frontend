import React, { useState } from 'react'
import PropTypes from 'prop-types'
import Header from './header';
import Footer from './footer';

const Landing = ({ children }) => children

export default Landing
